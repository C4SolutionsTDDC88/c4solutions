version: '3'
services:
  unit-front-end:
    container_name: front-end
    build:
      context: ../front-end
      dockerfile: Dockerfile-unittest
    volumes:
      - '../front-end:/front-end/'
      - /front-end/node_modules
    ports:
      - '9001:3001'
